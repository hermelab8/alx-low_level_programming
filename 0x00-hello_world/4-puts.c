#include <stdio.h>

/**
 * main - puts() function
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

	char Putstr[] = "\"Programming is like building a multilingual puzzle";

	puts(Putstr);
	return (0);
}
