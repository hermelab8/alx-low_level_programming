#include "main.h"

/**
 * _islower- checks lowercase character
 *@l: character to check
 *
 * Return: either 1 or 0
 */
int _islower(int l)
{
	if (l >= 97 && l <= 122)
	{
	return (1);
	}
	else
	return (0);
}
